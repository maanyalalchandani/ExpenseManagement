export const sendExpenseEmail = (expense) => {
    // Placeholder function to simulate sending an email
    console.log('Sending email with the following expense details:');
    console.log(`Date: ${expense.date}`);
    console.log(`Category: ${expense.category}`);
    console.log(`Amount: ${expense.amount}`);
  };
  